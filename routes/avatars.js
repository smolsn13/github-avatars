const express = require('express');
const router = express.Router();

// GET route to api/avatars
router.get('/', (req, res) => {
  url: 'https://api.github.com/repos/is:public'
});

module.exports = router;
